import SwiftUI

enum AppTheme {
    static let darkBlue = Color(red: 0.06, green: 0.14, blue: 0.30)
    static let panel = Color(red: 0.10, green: 0.18, blue: 0.36)
    static let textPrimary = Color.white
    static let textSecondary = Color.white.opacity(0.72)
}
